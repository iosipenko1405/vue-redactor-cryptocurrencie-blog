In order to start the application you should do the next steps:

1) Install Node.js, npm
2) Navigate to root of the project directory
3) Run `npm install`
4) Run `npm run build`
5) Open index.html in browser
